/**
 * Repository Layer for KPI Reviews
 * CRITICAL: This is the ONLY layer that should contain direct Supabase calls.
 * All business logic should be in the APIs layer.
 * This ensures clean migration to NestJS + PostgreSQL in the future.
 */

// ============================================================================
// KPI REVIEWS
// ============================================================================
